const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // if available, or just check what the env says

// Actually, I can just create an API route and curl it.
