import { notFound } from 'next/navigation';

// This page has been removed. A server component lets Next.js return a
// real HTTP 404 status immediately, instead of a client-rendered one.
export default function ChatbotPage() {
    notFound();
}
